# UI Redesign Plan

1. Audit current inline styles and layout structure.
2. Define global tokens (colors, spacing, typography) in `src/index.css`.
3. Create CSS Modules for auth screens and dashboard.
4. Replace inline styles with module classes.
5. Add responsive rules and refined states (hover, focus, empty).
6. Review for consistency and polish.
